# image-watermark
图片加水印脚本

python 3.10.14

## 使用

需要修改的参数：
```bash
font_path = "msyh.ttc"  # 字体路径
input_path = "input"  # 输入路径
output_path = "output"  # 输出路径
text = "水印文字"  # 水印文字
```

```bash
# 安装依赖
pip install pillow==12.1.0
# 直接运行
python main.py
```
